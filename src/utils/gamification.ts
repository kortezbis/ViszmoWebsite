/**
 * XP amounts for dashboard study flows (Learn Mode, etc.).
 * Wire persistence via DatabaseService.addXp when profile XP columns exist.
 */
export const XP_REWARDS = {
    CARD_MASTERED: 25,
} as const;
